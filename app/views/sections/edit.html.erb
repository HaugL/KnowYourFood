 <h3>Sections</h3>
<%=  simple_form_for :model, :url => article_section_create_path(@article), 
 					  :method => 'post' do |f| %>
 	  <%= f.label :title %>
	  <%= f.text_field :title, :label => 'Title' %>
	  <br>
	  <%= f.label :text %>
	  <%= f.text_area :text %>
	  <br>
	  <%= f.button :submit, 'Create Article' %>
 <% end %>


 <% if @article %>

 <% end %>

 <%= link_to 'Create the Article', articles_path%>